name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  release:
    types: [published]

env:
  PYTHON_VERSION: '3.11'
  DOCKER_IMAGE: devops-project-api

jobs:
  # Job 1: Linting et Tests
  test:
    name: 🧪 Linting & Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 pytest pytest-cov
      
      - name: 🔍 Lint with flake8
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # Exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        continue-on-error: true
      
      - name: 🧪 Run tests with pytest
        run: |
          pytest -v --cov=. --cov-report=term-missing --cov-report=xml
      
      - name: 📊 Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
        continue-on-error: true

  # Job 1.5: Security Checks (DevSecOps)
  security:
    name: 🛡️ Security Checks
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📦 Install Security Tools
        run: |
          pip install bandit pip-audit

      - name: 🕵️ Run Bandit (SAST)
        run: |
          bandit -r . -x ./venv,./test_api.py,./test_tracing.py
        continue-on-error: false

      - name: � Audit Dependencies
        run: |
          pip install -r requirements.txt
          pip-audit

  # Job 2: Build & Push Docker Image (Updated with Trivy)
  build-and-push:
    name: 🐳 Build, Scan & Push
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: 🔑 Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: nourmoussi
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: 🏷️ Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: nourmoussi/${{ env.DOCKER_IMAGE }}
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-
      
      # 1. Build and Load for Scanning
      - name: 🏗️ Build image for scanning
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: ${{ env.DOCKER_IMAGE }}:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 2. Run Trivy Vulnerability Scanner
      - name: 🛡️ Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ env.DOCKER_IMAGE }}:test'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      # 3. Build and Push (using cache)
      - name: 🚀 Build and push final image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: 📦 Image digest
        run: echo "Image pushed successfully! 🎉"

  # Job 3: DAST Scan with OWASP ZAP
  dast:
    name: �️ DAST (OWASP ZAP)
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # Utiliser l'image construite précédemment
      - name: 🐳 Pull Docker Image
        run: |
          docker pull nourmoussi/${{ env.DOCKER_IMAGE }}:latest
      
      - name: � Run Application
        run: |
          docker run -d -p 5000:5000 --name task-manager nourmoussi/${{ env.DOCKER_IMAGE }}:latest
          sleep 10 # Wait for app to initialize

      - name: 🧟 Run ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.11.0
        with:
          target: 'http://localhost:5000'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
          fail_action: false # Ne pas échouer le build pour l'instant (mode observation)
        continue-on-error: true
      
      - name: 📊 Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: zap-scan-report
          path: | 
            report_html.html
            report_json.json
            report_md.md
        continue-on-error: true
